cloud: on
server:
  port: 20001
eureka:
  instance:
     hostname: eureka1
  client:
     #检索服务
     fetch-registry: true
     # 是否注册自己
     register-with-eureka: true
     service-url:
       defaultZone: http://localhost:10001/eureka