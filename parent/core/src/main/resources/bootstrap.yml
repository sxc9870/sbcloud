spring:
  application:
    name: eureka-core
  profiles:
    active: test
  cloud:
    config:
      #这个要跟yml同名 后面active可以省略
      name: eurekaclient
      uri: http://localhost:8888   
management:
  endpoints:
    web:
      exposure:
        include: "*"
         