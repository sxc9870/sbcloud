spring:
  application:
    name: consume
  profiles:
    active: dev
  cloud:
    config:
      username: sxc
      password: 123456
      #这个要跟yml同名 后面active可以省略
      name: consume
      uri: http://localhost:8881
        
        
